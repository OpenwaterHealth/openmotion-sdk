"""Bundled laser-driver data: FPGA register map and register baselines.

Python modules, not data files, so they compile into the package with the
code (openmotion-sdk#278). Import the constants; do not parse anything.
"""
